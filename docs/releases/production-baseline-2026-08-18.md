# Production baseline — 2026-08-18

Снимок нужен, чтобы следующие сессии не путали три разных состояния сайта.

| Контур | Зафиксированное состояние |
|---|---|
| `origin/main` | `bd3c8fbd5aed9ef1992d13f5270c29c652de813e` |
| GitHub Pages | соответствует актуальному `origin/main` на момент аудита |
| `www.egoe-life.ru` на REG.RU | по проверенным ключевым файлам соответствует `8000cd13` |

Факты аудита:

- production DNS ведёт на REG.RU, а не на GitHub Pages;
- изменения PR #6/#7 присутствовали на GitHub Pages, но не были подтверждены на production;
- старый `deploy-webnames.yml` запускался только в dry-run и не являлся цепочкой публикации REG.RU;
- существовавшая публикация REG.RU выполнялась вручную из ZIP;
- до этого фундамента Pages публиковал корень репозитория вместе со служебными путями.

Этот файл не утверждает, что production навсегда остаётся на `8000cd13`. Перед каждым релизом состояние нужно проверить заново и создать новую запись.
